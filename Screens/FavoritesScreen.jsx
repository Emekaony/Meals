import { Text, StyleSheet } from "react-native";

const FavoritesScreen = () => {
  return <Text>This is the favorites screen</Text>;
};

const styles = StyleSheet.create({});

export default FavoritesScreen;
